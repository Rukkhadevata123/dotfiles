# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.

# These warnings indicate likely runtime problems with LTO, so promote them
# to errors. If a package fails to build with these, LTO should not be used there.
# WARNING_FLAGS="-Werror=odr -Werror=lto-type-mismatch -Werror=strict-aliasing"
# add -fPIC ?
# LDFLAGS="-Wl,--allow-shlib-undefined" emerge gnome-base/gnome-keyring ?

COMMON_FLAGS="-O2 -pipe -march=native -flto=thin ${WARNING_FLAGS}"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="-stdlib=libstdc++ ${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
LDFLAGS="-fuse-ld=lld ${COMMON_FLAGS} ${LDFLAGS}"

CC="clang"
CXX="clang++"
CPP="clang-cpp" # necessary for xorg-server and possibly other packages
LD="ld.lld"
AR="llvm-ar"
NM="llvm-nm"
RANLIB="llvm-ranlib"

# Fallback
# CC="gcc" CXX="g++" LD="ld.bfd" AR="ar" NM="nm" RANLIB="ranlib" CFLAGS="-O2 -pipe -march=native -flto" CXXFLAGS="-O2 -pipe -march=native -stdlib=libstdc++ -flto" LDFLAGS="-fuse-ld=bfd" emerge -av1 moonlight grub

RUSTFLAGS="${RUSTFLAGS} -C target-cpu=native -Clinker-plugin-lto"

MAKEOPTS="-j16 -l18"

ACCEPT_LICENSE="*"
ACCEPT_KEYWORDS="~amd64"

GENTOO_MIRRORS="http://distfiles.gentoo.org"

# Appending getbinpkg to the list of values within the FEATURES variable
FEATURES="${FEATURES} getbinpkg"
# Require signatures
FEATURES="${FEATURES} binpkg-request-signature"

# emerge
# EMERGE_DEFAULT_OPTS="--keep-going --with-bdeps=y --jobs=8 --load-average=10"
EMERGE_DEFAULT_OPTS="--keep-going --with-bdeps=y"

# USE
# NVIDIA GPU support (CUDA/NVENC/Driver)
USE="${USE} cuda nvenc nvidia"

# Video acceleration APIs (Intel/AMD/Open-Source)
USE="${USE} vaapi vdpau"

# Compiler toolchain optimizations (LLVM/LTO/PGO)
USE="${USE} clang lto pgo"

# Compression algorithms (High-speed/Low-latency)
USE="${USE} lz4 zstd"

# Kernel
USE="${USE} caps dist-kernel io-uring"

# Media framework
USE="${USE} ffmpeg"

# From kde
USE="${USE} dri pipewire policykit screencast"

ABI_X86="64"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C.utf8
